---
interface Props {
  href: string;
  title: string;
  blurb: string;
  badge?: string;
}
const { href, title, blurb, badge } = Astro.props;
---

<a
  href={href}
  class="group grid gap-2 rounded-[var(--radius-card)] border border-[color:var(--color-navy-100)] bg-white p-6 shadow-[var(--shadow-card)] transition hover:shadow-[var(--shadow-card-hover)]"
>
  {
    badge && (
      <span class="mb-1 self-start rounded-full bg-[color:var(--color-gold-100)] px-2 py-0.5 text-xs font-semibold tracking-wider text-[color:var(--color-gold-900)] uppercase">
        {badge}
      </span>
    )
  }
  <h3 class="text-xl">{title}</h3>
  <p class="text-sm text-[color:var(--color-ink-800)]">{blurb}</p>
  <span
    class="mt-2 text-sm font-semibold text-[color:var(--color-navy-700)] group-hover:text-[color:var(--color-gold-700)]"
    >Learn more →</span
  >
</a>
