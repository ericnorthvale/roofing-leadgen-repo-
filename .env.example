# ─── Northvale Roofing — env variables ───────────────────────────────────
# Copy this to `.env` for local dev. In prod, set these in Vercel Project Settings.
# Names mirror astro.config.mjs `env.schema`. Do not commit real values.

# Public (safe to ship to the browser)
PUBLIC_SITE_URL="https://northvaleroofing.com"
PUBLIC_BRAND_NAME="Northvale Roofing"
PUBLIC_GA4_ID=""                         # e.g. G-XXXXXXXXXX — from Task 10
PUBLIC_GTM_ID=""                         # e.g. GTM-XXXXXXX  — server-side container
PUBLIC_GSC_VERIFICATION=""               # Search Console HTML-tag token (content="..."); no DNS needed. See docs/analytics-search-console.md
PUBLIC_CALLRAIL_COMPANY_ID=""            # CallRail JS snippet id for DNI on the browser
META_PIXEL_ID=""                         # Meta pixel id for client-side pageview (CAPI is server-side, below)
GOOGLE_ADS_CONVERSION_ID=""              # AW-XXXXXXXXX

# Server secrets (set only in Vercel env, never commit)
ANTHROPIC_API_KEY=""                     # reuse your existing key from bleck-deal-engine
OPENAI_API_KEY=""                        # reuse from bart-app
HIGHLEVEL_API_KEY=""                     # from HighLevel → Settings → Private Integrations
HIGHLEVEL_LOCATION_ID=""                 # HighLevel sub-account (location) id
CALLRAIL_API_KEY=""                      # from CallRail → Integrations → API v3
CALLRAIL_ACCOUNT_ID=""
CALLRAIL_WEBHOOK_SECRET=""               # shared secret for /api/callrail-webhook signature verification (generate one in CallRail → Webhooks)
GOOGLE_MAPS_API_KEY=""                   # reuse from bleck-deal-engine if scoped; else new key
GOOGLE_PLACES_API_KEY=""                 # for /reviews live pull from GBP — see docs/setup-integrations.md
COMPANYCAM_API_KEY=""                    # CompanyCam API token for real job photos — see docs/setup-integrations.md
GOOGLE_PLACE_ID=""                       # Northvale Roofing GBP Place ID (find at https://developers.google.com/maps/documentation/places/web-service/place-id)
META_CAPI_TOKEN=""                       # Meta Conversions API access token (server-side pixel)

# Lead alerts — instant SMS + email on every lead (day-one safety net). See docs/setup-leads.md
TWILIO_ACCOUNT_SID=""                     # Twilio Account SID (starts AC...)
TWILIO_AUTH_TOKEN=""                      # Twilio Auth Token
TWILIO_FROM=""                            # Your Twilio number, E.164 e.g. +12815551234
LEAD_ALERT_SMS_TO=""                      # Owner cell(s), comma-separated E.164 e.g. +1281...,+1832...
RESEND_API_KEY=""                         # Resend API key (re_...)
LEAD_ALERT_EMAIL_TO=""                    # Owner email(s), comma-separated
LEAD_ALERT_EMAIL_FROM=""                  # Verified sender, e.g. leads@northvaleroofing.com

# Admin panel (/keystatic) — in-app Google sign-in. See docs/setup-admin-panel.md
ADMIN_ALLOWED_DOMAINS=""                   # optional; defaults to northvaleroofing.com
ADMIN_ALLOWED_EMAILS=""                    # optional; comma-separated specific emails
GOOGLE_OAUTH_CLIENT_ID=""                  # Google Cloud → OAuth 2.0 Client ID (Web application)
GOOGLE_OAUTH_CLIENT_SECRET=""              # Google Cloud → OAuth 2.0 Client secret
ADMIN_SESSION_SECRET=""                    # long random string (e.g. `openssl rand -base64 48`) — signs the admin session cookie
# These 4 are GENERATED by Keystatic's guided GitHub-App setup wizard — you don't
# invent them. Run setup once (see docs/setup-admin-panel.md), then paste all 4 here / into Vercel.
KEYSTATIC_GITHUB_CLIENT_ID=""             # generated by the Keystatic setup wizard
KEYSTATIC_GITHUB_CLIENT_SECRET=""         # generated by the Keystatic setup wizard
KEYSTATIC_SECRET=""                       # generated by the Keystatic setup wizard (signs the session)
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG=""       # generated by the Keystatic setup wizard (the GitHub App's slug; required client-side)
PUBLIC_KEYSTATIC_GITHUB_SETUP=""          # set to "true" ONLY while running the one-time setup wizard locally; remove afterward

# Optional — enable only when wired
VERCEL_ENV=""                            # auto-set by Vercel (production/preview/development)
